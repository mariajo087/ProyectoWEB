@{
    ViewData["Title"] = "Depositar";
}

<div class="container py-3 mb-5">

    <!-- Encabezado -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h4 class="fw-bold">Depositar</h4>
    </div>

    <!-- Barra de búsqueda -->
    <div class="input-group mb-4">
        <input type="text" class="form-control" placeholder="Destinatario">
        <button class="btn btn-outline-secondary" type="button">
            <i class="bi bi-search"></i> <!-- O usa un emoji: 🔍 -->
        </button>
    </div>

    <!-- Contacto sugerido -->
    <div class="d-flex align-items-center bg-light rounded p-3 mb-4">
        <img src="~/imagenes/user2.png" width="40" class="me-2" />
        <div>
            <div class="fw-bold">DIANA TAMAYO</div>
            <div class="text-muted">valenfernandez@hotmail.com</div>
        </div>
    </div>

    <!-- Botón Añadir Contactos (más pequeño) -->
    <div class="text-center mb-4">
        <button class="btn btn-success btn-sm fw-bold px-25">AÑADIR CONTACTOS</button>
    </div>

